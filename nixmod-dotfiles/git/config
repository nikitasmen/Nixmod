# Git config - delta, user (override in local if needed)
[user]
	name = nikitasmen
	email = menounosnikitas@gmail.com

[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[merge]
	conflictStyle = zdiff3

[delta]
	navigate = true
	dark = true
	line-numbers = true
